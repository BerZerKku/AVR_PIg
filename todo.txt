-. Проверить работу протокола MODBUS.
-. Проверить работу протокола IEC-101.
-. Проверить работу меню
	- Установка и отображение параметров
	- Отображение измерения
	- Отображение журналов
	- Работу с тестами
-. Проверить настройку и реальную работу Uart на доступных скоростях и разных настройках.
-. Учесть что при синхронизации времени теперь может пройти больше времени, т.к. увеличилось кол-во быстрых команд. Надо или сделать ее приоритетной или считать время до момента отправки.
-. Сетевые параметры пока меняются только в режиме Выведен (в БСП). Как надо?
-. Определиться с паролем для сброса до заводских настроек.
-. Если журнал не полный вывод начинается с 0 адреса, иначе с адреса последней (новой) записи?!
-. Разобраться с определением текущй записи в журналах. Проверить вывод всех журналов.
-. Починить тесты и добавить новые
-. Починить ввод параметров после изменения принципа ввода.
-. Разобраться с журналами безопасности. Почему может формироваться ошибка. 

-. Потеря связи с ПК (Link) определяется через несколько секунд. А должна, вроде, через секунду?!
	+ TIME_LOST_PC_CONNECTION 3, т.е. время до потери связи 3 секунды

-. Почему показывает 0 в журналах безопасности сообщений при отладке на ПК.
	+ Макс. количество событий не совпадает в debug и release. Поэтому при запуске разных версий PigPC и PigPC-BSP была ошибка. 

-. В режиме оператора можно зайти в Тесты, если их включили на ПК. При этом при выходе меняется режим.
	+ Добавлена проверка пользователя для уровня меню bool clMenu::checkUserReq(void (clMenu::*lvl)())
	+ Эта проверка добавлена при переходе на уровне Тест1, Тест2 и Журнал безопасности
	+ Эта проверка производится в цикле void clMenu::proc(void), для выхода с уровня при изменении пользователя

-. Код команды управления "Пуск АК" должен быть 4, а не 6.
	+ Сделано.

-. Проверить интервалы передачи команд с ПК. Время в конфигураторе иногда прыгает через 1 секунду.
	+ Так передает команды конфигуратор. На тесте с bvp61850 в 1 секунду стабильно проходят 5 команд.

-. Уточнить флаг переполнения журнала (0x8000 или 0xC000)
	+ Зависит от размера журнала. Проще отслеживать по маске 0xC000.
	+ Р400м - 0xC000. 

-. В записи журнала оптики миллисекунды идут старшим вперед, в остальных - младшим. Это правильно?!
	+ Это так.

-. Дополнить список доступных команд с ПК.
	+ Дополнено списком команд от Альберта + команды считывания журналов безопасности.

-. В конфигураторе используется команда 0x00. Проверить ее прохождение -> похоже не проходит.
	+ Команда проходила. Затык был на сервере БСП.

-. В конфигураторе используется команда 0x35 с доп байтом. Сделать так чтобы не мешала работе ПИ!
	+ Команда используется только в меню простмотра/установки параметров. Там ошибка помешать не успеет, т.к. перебьется нормальной командой.